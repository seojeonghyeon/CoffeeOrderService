{{- if .Values.service.enabled }}
{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.app.name }}
  namespace: {{ .Release.Namespace | quote }}
  {{- with .Values.podAnnotations }}
  annotations:
    version: {{- toYaml .version | nindent 8 }}
    build: {{- toYaml .build | nindent 8 }}
    environment: {{- toYaml .environment | nindent 8 }}
    monitoring_enabled: {{- toYaml .monitoring_enabled | nindent 8 }}
    monitoring_tool: {{- toYaml .monitoring_tool | nindent 8 }}
    log_config: {{- toYaml .log_config | nindent 8 }}
    deployment_pipeline: {{- toYaml .deployment_pipeline | nindent 8 }}
  {{- end }}
spec:
  rules:
    - http:
        paths:
          - backend:
              service:
                name: {{ .Values.app.name }}
                port:
                  number: {{ .Values.service.port }}
            pathType: {{ .Values.ingress.pathType }}
            path: {{ .Values.ingress.path }}
  ingressClassName: {{ .Values.ingress.className }}
{{- end }}
{{- end }}